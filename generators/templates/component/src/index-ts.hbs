import "@tmp/design/css";
import './{kebabCase name}.css';

export interface {{ pascalCase name }}Props {
  cn: string;
	label: string;
}

export function {{ pascalCase name }}(props: {{ titleCase }}Props) {
  return (
    <div className={props.cn || '{{ kebabCase name }}'}>
      <p>{props.label}</p>
    </div>
  )
}
